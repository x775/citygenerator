import json
import numpy as np

# change name to whatever later
def main(config):

    config["axiom"]["value"]
    config["border_y"]["value"]
    config["border_x"]["value"]

    # read config file
    # build initial state


if __name__ = '__main__':
    config = {}

    with open("roadnetwork.json", "r") as data:
        config = json.load(data)
    
    main(config)
