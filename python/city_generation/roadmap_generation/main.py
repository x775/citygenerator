from city_generation.config_loader import ConfigLoader

# change name to whatever later
def main(config):
    
    # read config file
    # build initial state


if __name__ = '__main__':
    config = ConfigLoaderd("roadnetwork")
    main(config)
